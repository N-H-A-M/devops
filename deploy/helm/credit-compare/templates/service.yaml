apiVersion: v1
kind: Service
metadata:
  name: {{ include "credit-compare.fullname" . }}
  labels:
    {{- include "credit-compare.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "credit-compare.selectorLabels" . | nindent 4 }}
