apiVersion: v1
kind: Service
metadata:
  name: backend-service
  namespace: application
spec:
  type: ClusterIP
  selector:
    app: backend  # Must match your Deployment's pod label
  ports:
    - protocol: TCP
      port: 80        # Port exposed inside cluster
      targetPort: 8000 # Port Uvicorn is listening on inside container