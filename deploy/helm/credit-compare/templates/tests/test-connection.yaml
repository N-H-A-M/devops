apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "credit-compare.fullname" . }}-test-connection"
  labels:
    {{- include "credit-compare.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "credit-compare.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
